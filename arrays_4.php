<?php

// There are built-in PHP functions for sorting arrays different ways.

// Sorting Indexed Arrays
$names = ["jenny", "peter", "bob", "joe"];
var_dump($names);

sort($names);
var_dump($names);

rsort($names);
var_dump($names);

